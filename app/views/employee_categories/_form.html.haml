= form_for @employee_category, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :name, :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'text_field'
  .control-group
    = f.label :prefix, :class => 'control-label'
    .controls
      = f.text_field :prefix, :class => 'text_field'
  .control-group
    = f.label :status, :class => 'control-label'
    .controls
      = f.check_box :status, :class => 'check_box'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), employee_categories_path, :class => 'btn'
